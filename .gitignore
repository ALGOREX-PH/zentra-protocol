# Dependencies
node_modules/
.pnpm-store/

# Rust / Soroban
target/
**/*.rs.bk
Cargo.lock.bak

# Build outputs
dist/
build/
*.tsbuildinfo

# Circom / snarkjs generated artifacts (regenerate via build scripts)
# circuits/ptau/ holds the downloaded, hash-pinned Hermez powers-of-tau cache
circuits/ptau/
*.ptau
*.zkey
*.r1cs
*.sym
*_js/
*_cpp/
witness.wtns
*.wtns
# Scratch output of circuits/spike/to-soroban.ts (the verifier crate does not embed it)
circuits/spike/spike_fixtures.rs

# Keep small, source-of-truth ZK artifacts (committed explicitly):
#   verification_key.json, proof.json, public.json, and generated vk.rs

# Secrets / local config
.demo-state.json
.env
.env.*
!.env.example
*.pem
.soroban/
.stellar/

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

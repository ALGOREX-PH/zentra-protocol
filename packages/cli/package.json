{
  "name": "@zentra/cli",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ALGOREX-PH/zentra-protocol.git",
    "directory": "packages/cli"
  },
  "type": "module",
  "description": "zentra — terminal tooling for ZK-guarded agent payments on Stellar. The bin is TypeScript executed via tsx (a devDependency): run `pnpm zentra` in this workspace.",
  "bin": {
    "zentra": "./src/index.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "zentra": "tsx src/index.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.2.0",
    "@zentra/sdk": "workspace:*",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
